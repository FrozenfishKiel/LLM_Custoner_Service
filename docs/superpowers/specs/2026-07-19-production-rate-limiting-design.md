# ????????

???2026-07-19

## ??

?????????????Redis Session??????chat ???Redis Tracker?Action ?????????????????? Redis key ???`rate:login:{ip}:{email_hash}`?`rate:register:{ip}`?`rate:email:{account_id}:{purpose}`?`rate:chat:{account_id}`???? HTTP route ??????????????????????????????????chat ????????????????

? spec ??????????????

## ??

1. ??? auth/chat HTTP route ???????????????????
2. ????????????????????????????Redis token issue?SMTP ???Agent/LLM ???????? Action?
3. ?????? Redis ?????? Lua ?????????????????
4. ?????????????????????????????????? pending/active?token ?????
5. Redis ?????????? Redis URL????key ???????

## ???

- ??? slice ? CAPTCHA?WAF??????????????????
- ?? legacy `/api/messages`?WebSocket inspect/debug ??????????????/?????????????? `/api/chat/*`?
- ???????? MySQL???????? Redis?
- ????? schema migration?
- ????? Prometheus exporter?? slice ????????????????? slice ???

## ????

### ?? A?route ?????

?? route ??? Redis key ? `INCR/EXPIRE`??????????????????????? route ???? TTL????????????????????

???????

### ?? B?????? FastAPI ???

?? slowapi ????????????????????? Redis Lua ?????/?????????????? key ?????????????????????+IP+?? hash ??????

????????

### ?? C????? Redis RateLimiter????

????????? `atguigu_ai/rate_limit/`??? `RedisRateLimiter`?`RateLimitRule`?`RateLimitDecision` ? `RateLimitExceeded`?HTTP route ???????? subject??? Redis ???TTL???????? limiter ??

????????????????????? Redis ????????????????????????

??????

## ????

### ????

?????? fixed window????????

- `name`??????? `auth.login.ip_email`?
- `limit`?????????
- `window_seconds`??????
- `subject`???????? IP?????? hash?account_id?
- `scope`??????? `auth`?`email`?`chat`?`action`?

?????? limiter ????

- `allowed: bool`
- `limit: int`
- `remaining: int`
- `retry_after_seconds: int`
- `reset_after_seconds: int`
- `rule_name: str`

??????? route?????? HTTP 429?

### Redis key

???? key ???????

```text
rate:{scope}:{rule_name}:{subject_hash_or_id}
```

???

- key ??????? email?token?message????session token ? CSRF token?
- email ?? `normalize_email()` ? SHA-256 hex digest?
- IP ????????????? key ???? hash??? IPv6 ???????????
- account_id/user_id ?????? ID?????? key?????????????? hash?

???

```text
rate:auth:register.ip:{ip_hash}
rate:auth:login.ip_email:{ip_hash}:{email_hash}
rate:auth:login.ip:{ip_hash}
rate:email:resend_verification.account:{account_id}
rate:email:forgot_password.ip_email:{ip_hash}:{email_hash}
rate:chat:messages.account:{account_id}
rate:action:critical_mutation.account:{account_id}
```

## ????

???????????????????????????????????????????????

| ?? | ?? | ???? | ???? | ???? |
| --- | --- | ---: | ---: | --- |
| ?? | IP | 1 ?? | 5 ? | ?? `AuthService.register` ? |
| ?? | IP + email hash | 15 ?? | 5 ? | ?? `AuthService.login` ? |
| ?? | IP | 15 ?? | 30 ? | ?? `AuthService.login` ? |
| ???? | IP + email hash | 1 ?? | 5 ? | ?? `AuthService.forgot_password` ? |
| ???? | IP | 1 ?? | 20 ? | ?? `AuthService.forgot_password` ? |
| ?????? | IP + email hash | 1 ?? | 5 ? | ?? `AuthService.resend_verification` ? |
| ???? | account_id + purpose | 1 ?? | 5 ? | ??????????????? |
| ???? | IP | 15 ?? | 60 ? | ?? `verify_email` ? |
| ???? | IP | 15 ?? | 20 ? | ?? `reset_password` ? |
| ??? | account_id | 15 ?? | 5 ? | CSRF ? session ?????? service ? |
| chat ?? | account_id | 1 ?? | 30 ? | ????????????? Agent ? |
| chat reset | account_id | 1 ?? | 10 ? | ???????????reset tracker ? |
| ????? Action | account_id + action_name | 1 ?? | 10 ? | Agent ?? Action ?? Action ??? |

???

- ???????? `IP+email` ? `IP` ???????????????
- ????????????????????/??????? route ????? IP/email ?????account_id ???? service ???????????????????????
- ????? Action ?????????????????????? Action ?????? `account_id` ? `action_name` ???????? Agent action dispatcher ?????

## HTTP ??

???????

```json
{
  "detail": "Too many requests"
}
```

HTTP ????`429 Too Many Requests`?

????

```text
Retry-After: <seconds>
X-RateLimit-Limit: <limit>
X-RateLimit-Remaining: <remaining>
X-RateLimit-Reset: <unix timestamp>
```

???

- ??? rule_name?subject?email hash?account_id?Redis key?
- ??????????????????????????????????????????
- ???????????????????????????????????????????? Agent ???

## IP ??

?????? `request.client.host`?

??????????????????????? CIDR ??????? `X-Forwarded-For` ? `X-Real-IP`????????????? header???????? IP ?????

?????

```text
TRUSTED_PROXY_CIDRS=
RATE_LIMIT_USE_FORWARDED_HEADERS=false
```

?????? slice ?????????????????

## Redis ????

`RedisRateLimiter.check(rule, subject)` ?? Lua ?????

1. ???? key ??????? string ????????????? store unavailable ??????????????
2. `INCR` ???
3. ????? 1???? `EXPIRE window_seconds`?
4. ?? TTL?
5. ?? `[allowed, count, ttl]`?

?????

- ?? key ????????? `limit`?
- TTL ?????????????????????????????????
- Redis ??????????TTL ??????? `RateLimitStoreUnavailable("Rate limit store is unavailable")`?

Redis ??????

- ??????????? fail closed??? 503 `"Rate limit service is unavailable"`?????????
- ???? chat ??? fail closed??? Redis ??? LLM/Agent ??????
- ?????? Redis ?????

## ??

?????????

```python
@dataclass(frozen=True)
class RateLimitSettings:
    enabled: bool
    fail_closed: bool
    rules: Mapping[str, RateLimitRule]
```

???????

```text
RATE_LIMIT_ENABLED=true
RATE_LIMIT_FAIL_CLOSED=true
RATE_LIMIT_REGISTER_IP_LIMIT=5
RATE_LIMIT_REGISTER_IP_WINDOW_SECONDS=3600
RATE_LIMIT_LOGIN_IP_EMAIL_LIMIT=5
RATE_LIMIT_LOGIN_IP_EMAIL_WINDOW_SECONDS=900
RATE_LIMIT_LOGIN_IP_LIMIT=30
RATE_LIMIT_LOGIN_IP_WINDOW_SECONDS=900
RATE_LIMIT_EMAIL_IP_EMAIL_LIMIT=5
RATE_LIMIT_EMAIL_IP_EMAIL_WINDOW_SECONDS=3600
RATE_LIMIT_EMAIL_ACCOUNT_LIMIT=5
RATE_LIMIT_EMAIL_ACCOUNT_WINDOW_SECONDS=3600
RATE_LIMIT_CHAT_ACCOUNT_LIMIT=30
RATE_LIMIT_CHAT_ACCOUNT_WINDOW_SECONDS=60
RATE_LIMIT_CHAT_RESET_ACCOUNT_LIMIT=10
RATE_LIMIT_CHAT_RESET_ACCOUNT_WINDOW_SECONDS=60
RATE_LIMIT_ACTION_MUTATION_ACCOUNT_LIMIT=10
RATE_LIMIT_ACTION_MUTATION_ACCOUNT_WINDOW_SECONDS=60
```

??????????

```text
RATE_LIMIT_ENABLED=false
```

?????????????????????????????? slice ????? `APP_ENV=production` ? `RATE_LIMIT_ENABLED=true`?

## ???

### Auth routes

`AuthRouteDependencies` ???? `rate_limiter` ? `client_ip_resolver`?????? limiter????????/????????? app wiring ?????

?????

- `register`??? IP ? ?? `register.ip` ? ?? service?
- `login`??? IP???? email/hash ? ?? `login.ip_email` ? `login.ip` ? ?? service?
- `forgot-password`?`resend-verification`??? IP???? email/hash ? ?? IP/email ?? ? ?? service?account_id ??????? service ??? email orchestration seam ???
- `verify-email`?`reset-password`??? IP ?????? token ??? key?
- `change-password`?? CSRF/session??? account_id ?????? service?

### Chat routes

?????

1. resolve session?
2. require CSRF?
3. resolve business identity?
4. ? account_id ?? chat ???
5. ?? message payload?
6. ?? Agent?

???message payload ??????????????????????????/??/???????????????? payload?

### Action critical mutation

????? Action ????????????? Action ????? `kwargs` ???? `account_id/user_id`?

- `ActionAskSetReceiveInfo`
- `ActionCancelOrder`
- `ActionApplyPostsale`

Action ????????????????????????

?? limiter ????Action ?????????????????????????/?????????????

## ???????

???? slice ?????????? slice ?? spec ??????

```text
rate_limit.check.total{rule,result}
rate_limit.blocked.total{rule}
rate_limit.store_unavailable.total{operation}
auth.route.rate_limited.total{route}
chat.route.rate_limited.total{route}
action.rate_limited.total{action_name}
```

?????

- INFO/DEBUG ??? rule_name?route?result?retry_after?
- ????? email?token?password?message?session token?CSRF token?
- account_id ??????????????? hash?

????????

- ???? 100% ?? 429?????? service/Agent/Action?
- Redis ???? 100% ???? 503 ? Action ?????
- ?????? key ???????? limit?
- ?? key TTL ??????????????????

## ????

### ????

?? `tests/unit/rate_limit/test_redis_rate_limiter.py`?

- ????? allowed?remaining ???TTL ???
- ?? limit ???? blocked?retry_after ?? TTL?
- Redis key ????/RedisError ??? `RateLimitStoreUnavailable`?????????
- subject ???? subject??? subject????????????? hash ??
- ?? email/token/password/message ?? key?

??/?? `tests/unit/api/test_auth_routes.py`?

- ?????? 429?service.register ????
- ?????????? 429?service.login ????cookies ????
- ????/?????????? 429?????????
- ???? session/CSRF ???? account_id ?????? service.change_password ????
- limiter ??????? 503?

??/?? `tests/unit/api/test_chat_routes.py`?

- chat messages ???? 429?Agent ????
- chat reset ???? 429?reset_tracker ????
- limiter ??????? 503?
- ????? metadata/account_id ????? subject?

?? `tests/unit/actions/test_action_rate_limit.py`?

- ????????????????????????????? audit?????????
- ???????? critical mutation ?????

### ????

?? `tests/integration/test_redis_rate_limiter.py`??? `llm-cs-redis` DB 15?

- ?? Redis ????? key???????? limit?
- TTL ????
- ???? DB15 ???

?? HTTP ?????

- auth route ?? Redis ?????
- chat route ?? Redis ?????
- ?? key ????? email/token/message?

### ????

??????????

```powershell
D:\Anaconda3\envsi-content-ops\python.exe -m pytest tests/unit/rate_limit tests/unit/api/test_auth_routes.py tests/unit/api/test_chat_routes.py -q
D:\Anaconda3\envsi-content-ops\python.exe -m pytest tests/integration/test_redis_rate_limiter.py -q -s -m integration
D:\Anaconda3\envsi-content-ops\python.exe -m pytest tests -q
```

## ?????

| ?? | ?? |
| --- | --- |
| Redis ?????????? 503 | fail closed ?????????? `rate_limit.store_unavailable.total` ??? |
| NAT/??????????? IP ?? | ???? IP+email ? IP ????IP ??????????chat ?? account_id |
| ????? X-Forwarded-For ???? | ???? forwarded header??????????? |
| ??????????? | ?????? 202/200 ? 429???????????? |
| key ??????? | email/token/message/password/session/csrf ??? key??????? |
| ?????? legacy demo | limiter ??? auth/chat deps ?????????????????????????????? |

## ????

1. ?????????????Redis key ????????
2. ???????? 429????? service/Agent/Action ????
3. Redis ????????????????????????
4. ??????????????????????????????????
5. ?? Redis ?????????? key ???? limit?
6. ???????Redis DB15 ??? MySQL ???? 0?
