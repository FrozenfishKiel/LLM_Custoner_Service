[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict
markers =
    integration: tests requiring application infrastructure or service adapters
    e2e: end-to-end tests that exercise complete user journeys
    evaluation: deterministic and model-assisted quality evaluations
    load: load, stress, soak, and capacity tests
